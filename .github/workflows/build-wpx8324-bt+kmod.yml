name: 'Build OpenWrt WPX8324-BT'

permissions:
  contents: write
  packages: write

on:
  workflow_dispatch:
    inputs:
      openwrt_version:
        description: 'Pilih Versi/Branch OpenWrt'
        required: true
        type: choice
        default: 'v25.12.5'
        options:
          - 'snapshot'
          - 'v25.12.5'
          - 'v25.12.4'
      custom_packages:
        description: 'Daftar Paket Tambahan (Gunakan tanda minus "-" untuk menghapus paket)'
        required: false
        type: string
        default: 'luci luci-ssl kmod-usb-net kmod-usb-net-qmi-wwan uqmi luci-proto-qmi kmod-usb-serial-option kmod-usb-serial kmod-usb-serial-wwan qmi-utils kmod-usb-acm kmod-usb-net-cdc-ncm kmod-usb-net-cdc-mbim umbim modemmanager luci-app-modemmanager usbutils iperf3 nano htop'

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - name: 1. Checkout Kode Repositori (Mengambil repo kamu)
        uses: actions/checkout@v4

      - name: 2. Siapkan Variabel Build
        id: build_vars
        run: echo "BUILD_VERSION=${{ github.event.inputs.openwrt_version }}" >> $GITHUB_ENV

      - name: 3. Kosongkan Ruang Disk
        run: |
          sudo rm -rf /usr/share/dotnet /usr/local/lib/android /opt/ghc /usr/local/.ghcup
          sudo apt-get clean
          df -h

      - name: 4. Instal Dependensi Build
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            build-essential clang flex bison g++ gawk gcc-multilib gettext \
            git libncurses5-dev libssl-dev python3-setuptools rsync unzip \
            zlib1g-dev file wget ccache curl

      - name: 5. Clone Repositori OpenWrt
        run: git clone https://github.com/openwrt/openwrt.git ./openwrt

      - name: 6. Atur Cache Build (Nuke Cache v16)
        uses: actions/cache@v4
        with:
          path: |
            openwrt/dl
            openwrt/.ccache
          key: ${{ runner.os }}-openwrt-qualcommax-${{ github.event.inputs.openwrt_version }}-v16
          restore-keys: |
            ${{ runner.os }}-openwrt-qualcommax-${{ github.event.inputs.openwrt_version }}-v16-

      - name: 7. Injeksi Kode (Jalur Mandiri File Lokal)
        working-directory: ./openwrt
        run: |
          echo "BUILD_DATE=$(date +%Y-%m-%d)" >> $GITHUB_ENV
          
          git checkout ${{ github.event.inputs.openwrt_version }}
          
          mkdir -p target/linux/qualcommax/files/arch/arm64/boot/dts/qcom/
          mkdir -p files/etc/hotplug.d/firmware/
          
          cp $GITHUB_WORKSPACE/custom-files/ipq5018-wpx8324.dts target/linux/qualcommax/files/arch/arm64/boot/dts/qcom/
          cp $GITHUB_WORKSPACE/custom-files/11-ath11k-caldata files/etc/hotplug.d/firmware/
          
          cat << 'EOF' >> target/linux/qualcommax/image/ipq50xx.mk

          define Device/wpx8324_ap-mp03-5-c1
            $(call Device/FitImageLzma)
            $(call Device/UbiFit)
            DEVICE_VENDOR := Lite-On
            DEVICE_MODEL := WPX8324
            DEVICE_DTS := ipq5018-wpx8324
            DEVICE_DTS_CONFIG := config@mp03.5-c1
            SOC := ipq5018
            BLOCKSIZE := 128k
            PAGESIZE := 2048
            IMAGE_SIZE := 59392k
            NAND_SIZE := 128m
            DEVICE_PACKAGES := ath11k-firmware-ipq5018-qcn6122 ipq-wifi-yuncore_ax830 kmod-usb-dwc3-qcom kmod-usb-storage kmod-fs-vfat block-mount usbutils
          endef
          TARGET_DEVICES += wpx8324_ap-mp03-5-c1
          EOF

          sed -i 's/yuncore,ax830|\\/wpx8324,ap-mp03-5-c1|\\\n\tyuncore,ax830|\\/g' target/linux/qualcommax/ipq50xx/base-files/lib/upgrade/platform.sh
          sed -i 's/yuncore,ax850)/wpx8324,ap-mp03-5-c1)\n\t\tucidef_set_interfaces_lan_wan "lan" "wan"\n\t\t;;\n\tyuncore,ax850)/g' target/linux/qualcommax/ipq50xx/base-files/etc/board.d/02_network
          sed -i 's/yuncore,ax830|\\/wpx8324,ap-mp03-5-c1|\\\n\tyuncore,ax830|\\/g' package/boot/uboot-tools/uboot-envtools/files/qualcommax_ipq50xx
          
          for i in 1 2 3; do
            ./scripts/feeds update -a && break || sleep 60
          done
          ./scripts/feeds install -a

      - name: 8. Konfigurasi First Boot (uci-defaults)
        working-directory: ./openwrt
        run: |
          mkdir -p files/etc/uci-defaults
          cat << 'EOF' > files/etc/uci-defaults/99-custom-settings
          #!/bin/sh
          uci set system.@system[0].timezone='WIB-7'
          uci set system.@system[0].zonename='Asia/Jakarta'
          uci set system.@system[0].hostname='WPX8324-BT'
          uci commit system
          
          uci set wireless.default_radio0.ssid='WPX8324-BT'
          uci set wireless.radio0.country='ID'
          uci set wireless.radio0.disabled='0'
          uci set wireless.radio0.channel='auto'
          
          if uci get wireless.radio1 > /dev/null 2>&1; then
            uci set wireless.default_radio1.ssid='WPX8324-BT 5G'
            uci set wireless.radio1.disabled='0'
            uci set wireless.radio1.channel='auto'
          fi
          uci commit wireless
          sed -i -e '/12d1:15c1/,+5d' /etc/usb-mode.json
          sed -i -e '/413c:81d7/,+5d' /etc/usb-mode.json
          wifi reload
          wifi up
          exit 0
          EOF
          chmod +x files/etc/uci-defaults/99-custom-settings

      - name: 9. Terapkan Konfigurasi Kustom & Kmods
        working-directory: ./openwrt
        run: |
          echo 'CONFIG_CCACHE=y' > .config
          echo 'CONFIG_TARGET_qualcommax=y' >> .config
          echo 'CONFIG_TARGET_qualcommax_ipq50xx=y' >> .config
          echo '# CONFIG_TARGET_qualcommax_ipq807x is not set' >> .config
          echo 'CONFIG_TARGET_MULTI_PROFILE=y' >> .config
          echo 'CONFIG_TARGET_DEVICE_qualcommax_ipq50xx_DEVICE_wpx8324_ap_mp03_5_c1=y' >> .config
          
          # Ini adalah kunci untuk membangkitkan semua kernel module!
          echo 'CONFIG_ALL_KMODS=y' >> .config
          
          CUSTOM_PKGS="${{ github.event.inputs.custom_packages }}"
          if [ -n "$CUSTOM_PKGS" ]; then
            for pkg in $CUSTOM_PKGS; do
              if [[ $pkg == -* ]]; then
                clean_pkg="${pkg#-}"
                echo "# CONFIG_PACKAGE_$clean_pkg is not set" >> .config
              else
                echo "CONFIG_PACKAGE_$pkg=y" >> .config
              fi
            done
          fi
          make defconfig

      - name: 10. Mulai Proses Build
        working-directory: ./
        env:
          CCACHE_DIR: ${{ github.workspace }}/openwrt/.ccache
          USE_CCACHE: 1
        run: |
          make -j$(nproc) -C openwrt download
          make -j$(nproc) -C openwrt world || make -j1 V=s -C openwrt world

      - name: 11. Unggah Artefak Build (Otomatis ZIP)
        uses: actions/upload-artifact@v4
        with:
          name: WPX8324-Firmware-and-Packages-${{ github.event.inputs.openwrt_version }}-${{ env.BUILD_DATE }}
          path: |
            ./openwrt/bin/targets/qualcommax/ipq50xx/*wpx8324*
            ./openwrt/bin/targets/qualcommax/ipq50xx/kmods/
            ./openwrt/bin/packages/

      - name: 12. Kumpulkan Berkas untuk Rilis
        run: |
          mkdir -p ./release_files
          
          # Pindahkan file firmware bin/img
          find ./openwrt/bin/targets/qualcommax/ipq50xx/ -type f -name "*wpx8324*" -exec cp {} ./release_files/ \; 2>/dev/null || true
          
          # Bungkus seluruh KMODS ke dalam satu file .tar.gz
          if [ -d "./openwrt/bin/targets/qualcommax/ipq50xx/kmods" ]; then
            echo "Membuat arsip KMODS..."
            tar -czvf ./release_files/kmods-ipq50xx-${{ env.BUILD_DATE }}.tar.gz -C ./openwrt/bin/targets/qualcommax/ipq50xx/ kmods/
          fi
          
          # Bungkus seluruh Packages arsitektur (opsional untuk dependensi aplikasi)
          if [ -d "./openwrt/bin/packages" ]; then
            echo "Membuat arsip Packages umum..."
            tar -czvf ./release_files/packages-aarch64-${{ env.BUILD_DATE }}.tar.gz -C ./openwrt/bin/ packages/
          fi

      - name: 13. Kirim Berkas ke GitHub Release
        uses: ncipollo/release-action@v1
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          name: "OpenWrt WPX8324 (IPQ50xx) ${{ github.event.inputs.openwrt_version }} (${{ env.BUILD_DATE }})"
          tag: "${{ github.event.inputs.openwrt_version }}-${{ env.BUILD_DATE }}"
          artifacts: "./release_files/*"
          allowUpdates: true
          replacesArtifacts: true
          prerelease: false
