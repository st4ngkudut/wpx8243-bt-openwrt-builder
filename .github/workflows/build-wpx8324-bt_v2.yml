name: 'Build OpenWrt WPX8324-BT V2'

permissions:
  contents: write
  packages: write

on:
  workflow_dispatch:
    inputs:
      openwrt_version:
        description: 'Pilih Versi/Branch OpenWrt'
        required: true
        type: choice
        default: 'snapshot'
        options:
          - 'snapshot'
          - 'v23.05.5'
          - 'v23.05.4'
      custom_packages:
        description: 'Daftar Paket Tambahan (Opsional, gunakan "-" untuk menghapus paket)'
        required: false
        type: string
        default: ''

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - name: 1. Checkout Kode Repositori
        uses: actions/checkout@v4

      - name: 2. Siapkan Variabel Build
        id: build_vars
        run: echo "BUILD_VERSION=${{ github.event.inputs.openwrt_version }}" >> $GITHUB_ENV

      - name: 3. Kosongkan Ruang Disk
        run: |
          sudo rm -rf /usr/share/dotnet /usr/local/lib/android /opt/ghc /usr/local/.ghcup
          sudo apt-get clean
          df -h

      - name: 4. Instal Dependensi Build
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            build-essential clang flex bison g++ gawk gcc-multilib gettext \
            git libncurses5-dev libssl-dev python3-setuptools rsync unzip \
            zlib1g-dev file wget ccache curl zstd

      - name: 5. Clone Repositori OpenWrt
        run: git clone https://github.com/openwrt/openwrt.git ./openwrt

      - name: 6. Atur Cache Build
        uses: actions/cache@v4
        with:
          path: |
            openwrt/dl
            openwrt/.ccache
          key: ${{ runner.os }}-openwrt-qualcommax-${{ github.event.inputs.openwrt_version }}-v1
          restore-keys: |
            ${{ runner.os }}-openwrt-qualcommax-${{ github.event.inputs.openwrt_version }}-v1-

      - name: 7. Terapkan Pull Request #24227 (Anti-Gagal)
        working-directory: ./openwrt
        run: |
          echo "BUILD_DATE=$(date +%Y-%m-%d)" >> $GITHUB_ENV
          
          # Tangani perbedaan nama branch "snapshot" ke "main" di Git
          if [ "${{ github.event.inputs.openwrt_version }}" == "snapshot" ]; then
            git checkout main
          else
            git checkout ${{ github.event.inputs.openwrt_version }}
          fi
          
          echo "Menerapkan Pull Request #24227 (Lite-On WPX8324)..."
          curl -sL https://github.com/openwrt/openwrt/pull/24227.patch > pr24227.patch
          
          # Gunakan patch -p1 sebagai fallback paling aman jika git apply konflik whitespace
          git apply --3way pr24227.patch || patch -p1 < pr24227.patch || echo "⚠️ Beberapa patch mungkin gagal diterapkan (Cek Log)"
          
          # Update dan install feeds
          for i in 1 2 3; do
            ./scripts/feeds update -a && break || sleep 60
          done
          ./scripts/feeds install -a

      - name: 8. Sinkronisasi Vermagic Kernel
        working-directory: ./openwrt
        run: |
          VERSION="${{ github.event.inputs.openwrt_version }}"
          VERSION_NUM=$(echo "$VERSION" | sed 's/^v//')
          
          if [[ "$VERSION" == "snapshot" ]]; then
            KMODS_URL="https://downloads.openwrt.org/snapshots/targets/qualcommax/ipq50xx/kmods/"
            PACKAGES_URL="https://downloads.openwrt.org/snapshots/targets/qualcommax/ipq50xx/packages/Packages"
          else
            KMODS_URL="https://downloads.openwrt.org/releases/${VERSION_NUM}/targets/qualcommax/ipq50xx/kmods/"
            PACKAGES_URL="https://downloads.openwrt.org/releases/${VERSION_NUM}/targets/qualcommax/ipq50xx/packages/Packages"
          fi
          
          FULL_KERNEL_VER=$(curl -sL "$KMODS_URL" | grep -oE '[0-9]+\.[0-9]+\.[0-9]+-[0-9]+-[a-f0-9]{32}' | head -n 1)
          if [[ -z "$FULL_KERNEL_VER" ]]; then
            FULL_KERNEL_VER=$(curl -sL "$PACKAGES_URL" | awk '/^Package: kernel$/{f=1} f && /^Version:/{print $2; exit}')
          fi
          
          if [[ -n "$FULL_KERNEL_VER" ]]; then
            VERMAGIC=$(echo "$FULL_KERNEL_VER" | awk -F'-' '{print $NF}')
            echo "$VERMAGIC" > .vermagic
            sed -i 's|grep '\''=\[ym\]'\'' $(LINUX_DIR)/.config.set \| LC_ALL=C sort \| $(MKHASH) md5 > $(LINUX_DIR)/.vermagic|cp $(TOPDIR)/.vermagic $(LINUX_DIR)/.vermagic|g' ./include/kernel-defaults.mk
            echo "✅ Berhasil menyuntikkan Vermagic Official: $VERMAGIC"
          else
            echo "⚠️ Peringatan: Gagal menemukan Vermagic dari server OpenWrt!"
          fi

      - name: 9. Konfigurasi First Boot (uci-defaults)
        working-directory: ./openwrt
        run: |
          mkdir -p files/etc/uci-defaults
          cat << 'EOF' > files/etc/uci-defaults/99-custom-settings
          #!/bin/sh
          uci set system.@system[0].timezone='WIB-7'
          uci set system.@system[0].zonename='Asia/Jakarta'
          uci set system.@system[0].hostname='WPX8324-BT'
          uci commit system
          
          uci set wireless.default_radio0.ssid='WPX8324-BT'
          uci set wireless.radio0.country='ID'
          uci set wireless.radio0.disabled='0'
          uci set wireless.radio0.channel='auto'
          
          if uci get wireless.radio1 > /dev/null 2>&1; then
            uci set wireless.default_radio1.ssid='WPX8324-BT 5G'
            uci set wireless.radio1.disabled='0'
            uci set wireless.radio1.channel='auto'
          fi
          uci commit wireless
          sed -i -e '/12d1:15c1/,+5d' /etc/usb-mode.json
          sed -i -e '/413c:81d7/,+5d' /etc/usb-mode.json
          wifi reload
          wifi up
          exit 0
          EOF
          chmod +x files/etc/uci-defaults/99-custom-settings

      - name: 10. Terapkan Konfigurasi Kustom
        working-directory: ./openwrt
        run: |
          echo 'CONFIG_CCACHE=y' > .config
          echo 'CONFIG_TARGET_qualcommax=y' >> .config
          echo 'CONFIG_TARGET_qualcommax_ipq50xx=y' >> .config
          echo '# CONFIG_TARGET_qualcommax_ipq807x is not set' >> .config
          echo 'CONFIG_TARGET_MULTI_PROFILE=y' >> .config
          
          # PASTIKAN ID INI SAMA DENGAN NAMA DEVICE DI DALAM PR #24227
          echo 'CONFIG_TARGET_DEVICE_qualcommax_ipq50xx_DEVICE_wpx8324_ap-mp03-5-c1=y' >> .config
          
          # --- DAFTAR PAKET WAJIB (Akan selalu diinstal) ---
          DEFAULT_PKGS="luci luci-ssl kmod-usb-net kmod-usb-net-qmi-wwan uqmi luci-proto-qmi kmod-usb-serial-option kmod-usb-serial kmod-usb-serial-wwan qmi-utils kmod-usb-acm kmod-usb-net-cdc-ncm kmod-usb-net-cdc-mbim umbim modemmanager luci-app-modemmanager usbutils iperf3 nano htop"
          for pkg in $DEFAULT_PKGS; do
            echo "CONFIG_PACKAGE_$pkg=y" >> .config
          done
          
          # --- DAFTAR PAKET TAMBAHAN (Dari Input Opsional) ---
          CUSTOM_PKGS="${{ github.event.inputs.custom_packages }}"
          if [ -n "$CUSTOM_PKGS" ]; then
            for pkg in $CUSTOM_PKGS; do
              if [[ $pkg == -* ]]; then
                clean_pkg="${pkg#-}"
                echo "# CONFIG_PACKAGE_$clean_pkg is not set" >> .config
              else
                echo "CONFIG_PACKAGE_$pkg=y" >> .config
              fi
            done
          fi
          make defconfig

      - name: 11. Mulai Proses Build
        working-directory: ./
        env:
          CCACHE_DIR: ${{ github.workspace }}/openwrt/.ccache
          USE_CCACHE: 1
        run: |
          make -j$(nproc) -C openwrt download
          make -j$(nproc) -C openwrt world || make -j1 V=s -C openwrt world

      - name: 12. Unggah Artefak Build
        uses: actions/upload-artifact@v4
        with:
          name: WPX8324-Firmware-${{ github.event.inputs.openwrt_version }}-${{ env.BUILD_DATE }}
          path: ./openwrt/bin/targets/qualcommax/ipq50xx/*wpx8324*

      - name: 13. Kumpulkan Berkas untuk Rilis
        run: |
          mkdir -p ./release_files
          find ./openwrt/bin/targets/qualcommax/ipq50xx/ -type f -name "*wpx8324*" -exec cp {} ./release_files/ \; 2>/dev/null || true

      - name: 14. Kirim Berkas ke GitHub Release
        uses: ncipollo/release-action@v1
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          name: "OpenWrt WPX8324 (IPQ50xx) ${{ github.event.inputs.openwrt_version }} (${{ env.BUILD_DATE }})"
          tag: "${{ github.event.inputs.openwrt_version }}-${{ env.BUILD_DATE }}"
          artifacts: "./release_files/*"
          allowUpdates: true
          replacesArtifacts: true
          prerelease: false
